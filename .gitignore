# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pythonlibs/
uv.lock

# Virtual environments
.venv
venv/
ENV/
env/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
dist/
.vite/

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# Logs
/tmp/logs/

# OS
.DS_Store
Thumbs.db

# Runtime SQLite databases (chat cache + seeded data, regenerated at runtime)
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# Claude local files
.local/
attached_assets/

# Web-push VAPID secrets + subscriptions (never commit)
backend/vapid_private.pem
backend/vapid_public.txt
backend/data/push_subscriptions.json

# Phase 17B personalisation — dev JSON fallback stores (user behaviour data)
data/user_behaviour.json
data/saved_trails_server.json
data/notification_prefs.json

# Local safety snapshot of cleared seed data (not for the repo — recoverable via git history)
.cleanup_backup_*/
